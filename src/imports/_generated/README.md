## Generated imports quarantine

Place raw or generated Figma exports here during migration.

These files are useful as reference material, but they should not become the
main public component layer for the Make Kit.

Use this folder for:

- generated components
- generated token JSON
- temporary migration artifacts

Use `src/SvsUiNova/components/` for the curated, supported layer.
