# Global Tokens

Place shared cross-product token payloads here.

Expected primary artifacts:

- `colors.json` for exported global colors

These files represent shared base values that can feed primitive or semantic
layers, but components should still consume semantic theme tokens where
possible.
