## App shell

Place the Make Kit demo and app entrypoints here, for example:

- `App.tsx`
- `ComponentDemo.tsx`
- `AllComponentsDemo.tsx`

This surface is useful for verifying that every component status in the
manifest is reflected in a working demo state.
